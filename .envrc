# direnv config — auto-pins Claude Code to the OpenCraft account
# when you cd into this repo. Requires direnv (`brew install direnv`).
#
# After installing direnv, run once: `direnv allow`
export CLAUDE_CONFIG_DIR="$HOME/.claude-opencraft"
